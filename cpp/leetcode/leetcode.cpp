#include "../everything.h"

class Solution {
public:
  int fn() {
    return 0;
  }
};

int main() {
  Solution s;

  // initialise input

  // call function
  auto ans = s.fn();

  // assert output
  assert(ans == 0);
}