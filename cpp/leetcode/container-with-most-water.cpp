#include "../everything.h"

class Solution {
public:
  // 
  int maxArea(vector<int>& height) {

  }
};

int main() {
  Solution s;

  // initialise input

  // call function
  auto ans = s.fn();

  // assert output
  assert(ans == 0);
}